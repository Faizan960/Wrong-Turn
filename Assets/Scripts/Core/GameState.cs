namespace WrongDirection.Core
{
    public enum GameState
    {
        Boot,
        Menu,
        Tutorial,
        Playing,
        Paused,
        GameOver,
        Settings,
        Leaderboard
    }
}
